{
  "name": "idphoto-kit",
  "version": "0.0.0",
  "private": true,
  "description": "Privacy-first AI ID photo toolkit that runs entirely in the browser",
  "license": "MIT",
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "echo \"dev: pending packages/web (T1.10)\" && exit 0",
    "build": "echo \"build: pending packages/web (T1.10)\" && exit 0",
    "lint": "biome check .",
    "test": "vitest run",
    "specs:check": "pnpm --filter @idphoto-kit/specs check"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
